# Firebase
FIREBASE_PROJECT_ID=your-project-id

# TikTok Shop Partner API
# Client key do TikTok Shop Partner (obtido no Partner Center)
TIKTOK_CLIENT_KEY=your-client-key
# Client secret do TikTok Shop Partner (obtido no Partner Center)
TIKTOK_CLIENT_SECRET=your-client-secret
# Alternativa: TIKTOK_APP_SECRET (mantido para compatibilidade)
TIKTOK_APP_SECRET=your-app-secret

# TikTok Webhook
# Secret usado para validar a assinatura dos webhooks do TikTok
TIKTOK_WEBHOOK_SECRET=your-webhook-secret

# TikTok Encryption
# Chave para criptografar tokens OAuth (32 bytes em base64)
# Gerar com: openssl rand -base64 32
TIKTOK_ENCRYPTION_KEY=your-32-byte-base64-encryption-key

# Frontend URL (para redirecionamento após OAuth)
FRONTEND_URL=http://localhost:3000

# Telegram
# Token padrão do bot Telegram (opcional, pode ser configurado por usuário)
TELEGRAM_BOT_TOKEN=your-default-bot-token

# Kiwify
# Chave secreta para validar assinatura HMAC dos webhooks da Kiwify
# A assinatura é validada usando HMAC SHA1 do payload JSON
# Chave secreta fornecida pela Kiwify: 3ienivdzi7c
KIWIFY_SECRET_KEY=3ienivdzi7c
# (Alternativa: KIWIFY_WEBHOOK_TOKEN ainda funciona para compatibilidade)

# Email (SMTP)
# Configurações para envio de emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Configurações do Ambiente
# Valores possíveis: development, staging, production
NODE_ENV=development

